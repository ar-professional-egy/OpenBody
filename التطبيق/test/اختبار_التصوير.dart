import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('اختبار شاشة التصوير', (WidgetTester tester) async {
    // اختبار واجهة التصوير
    expect(true, true); // اختبار أساسي
  });
}
